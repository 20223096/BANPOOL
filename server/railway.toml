# Railway: 서비스 설정에서 Root Directory를 이 폴더(server)로 지정하세요.
# 모노레포 루트에서 배포하면 concurrently로 웹+API가 동시에 뜨며 PORT 충돌이 납니다.

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
